"use client";import{use,useState}from"react";export default function ResetClient({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=""}=use(searchParams);const[p,setP]=useState("");const[s,setS]=useState("");async function go(){const r=await fetch('/api/account/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password:p})});const d=await r.json();setS(d.message||d.error);if(r.ok)setTimeout(()=>location.href='/login',1000)}return <section className="narrow-card"><h1>Choose a new password</h1><input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="New password"/><button className="primary-action" onClick={go}>Update password</button><p>{s}</p></section>}
