"use client";import{useState}from"react";export default function ForgotClient(){const[email,setEmail]=useState("");const[s,setS]=useState("");async function go(){const r=await fetch('/api/account/forgot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setS(d.message||d.error)}return <section className="narrow-card"><p className="eyebrow">ACCOUNT RECOVERY</p><h1>Reset your password</h1><p>Enter your email. If an account exists, a time-limited reset link will be sent.</p><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.fi"/><button className="primary-action" onClick={go}>Send reset link</button><p>{s}</p></section>}
