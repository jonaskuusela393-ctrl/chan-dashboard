import{redirect}from"next/navigation";import{getSession}from"@/lib/auth";import PortalClient from'./PortalClient';export const metadata={title:'Customer workspace',robots:{index:false,follow:false}};export default async function Page(){const s=await getSession();if(!s)redirect('/login');if(s.role==='admin')redirect('/dashboard');if(s.role!=='customer')redirect('/chat');return <PortalClient user={{name:s.username,email:s.email||''}}/>}
