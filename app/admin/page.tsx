import{redirect}from'next/navigation';import{getSession}from'@/lib/auth';import AdminClient from'./AdminClient';export const metadata={title:'Platform administration',robots:{index:false,follow:false}};export default async function Page(){const s=await getSession();if(!s)redirect('/login');if(s.role!=='admin')redirect(s.role==='customer'?'/portal':'/chat');return <AdminClient/>}
