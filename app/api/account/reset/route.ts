import{NextRequest,NextResponse}from"next/server";import{resetPassword}from"@/lib/accounts";export async function POST(req:NextRequest){try{const b=await req.json();const ok=await resetPassword(String(b.token||''),String(b.password||''));return NextResponse.json(ok?{ok:true,message:'Password updated.'}:{ok:false,error:'The reset link is invalid or expired.'},{status:ok?200:400})}catch(e:any){return NextResponse.json({ok:false,error:e.message},{status:400})}}
