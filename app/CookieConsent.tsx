"use client";import{useEffect,useState}from'react';export default function CookieConsent(){const[show,setShow]=useState(false);useEffect(()=>setShow(!localStorage.getItem('raccoon-analytics-consent')),[]);if(!show)return null;function set(v:string){localStorage.setItem('raccoon-analytics-consent',v);setShow(false);if(v==='yes')location.reload()}return <div className="consent-banner"><div><strong>Optional visitor statistics</strong><p>We use anonymous, first-party statistics only with your permission. No raw IP address is stored.</p></div><div className="row"><button onClick={()=>set('no')}>Decline</button><button className="primary-action" onClick={()=>set('yes')}>Allow statistics</button></div></div>}
